openapi: 3.0.0
info:
  title: Internal Settings Company
  version: v1.0.0
paths:
  /api/internal/settings/companies/default_addresses:
    patch:
      summary: default_addresses  default addresses internal settings companies
