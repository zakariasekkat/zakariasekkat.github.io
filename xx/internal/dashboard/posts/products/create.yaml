openapi: 3.0.0
info:
  title: Internal Dashboard Posts Product
  version: v1.0.0
paths:
  /api/internal/dashboard/posts/:post_id/products:
    post:
      summary: create  
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                product:
                  type: object
                  properties:
                    product_id:
                      type: string
