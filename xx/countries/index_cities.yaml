openapi: 3.0.0
info:
  title: Country
  version: v1.0.0
paths:
  /api/countries/:id/regions/:region_id/cities:
    get:
      summary: index_cities  
      responses:
        200:
          description: 'success'
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  name:
                    type: string
