openapi: 3.0.0
info:
  title: Admin Product tags item
  version: v1.0.0
paths:
  /api/admin/product_tags_items:
    post:
      summary: create  
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                product_tags_item:
                  type: object
                  properties:
                    dictionary_item_id:
                      type: string
                    product_id:
                      type: string
