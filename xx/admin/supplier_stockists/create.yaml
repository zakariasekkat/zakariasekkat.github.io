openapi: 3.0.0
info:
  title: Admin Supplier stockist
  version: v1.0.0
paths:
  /api/admin/supplier_stockists:
    post:
      summary: create  
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                supplier_stockist:
                  type: object
                  properties:
                    stockist_supplier_id:
                      type: string
                    supplier_id:
                      type: string
