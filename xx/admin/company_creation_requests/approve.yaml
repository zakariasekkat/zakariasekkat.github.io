openapi: 3.0.0
info:
  title: Admin Company creation request
  version: v1.0.0
paths:
  /api/admin/company_creation_requests/:id/approve:
    patch:
      summary: approve  approve admin company creation request
