openapi: 3.0.0
info:
  title: Admin Company creation request
  version: v1.0.0
paths:
  /api/admin/company_creation_requests/:id/decline:
    patch:
      summary: decline  decline admin company creation request
