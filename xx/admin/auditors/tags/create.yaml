openapi: 3.0.0
info:
  title: Admin Auditors Tag
  version: v1.0.0
paths:
  /api/admin/auditors/:auditor_id/tags:
    post:
      summary: create  
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                tag:
                  type: object
                  properties:
                    dictionary_item_id:
                      type: string
                    auditor_id:
                      type: string
