openapi: 3.0.0
info:
  title: Admin Auditor
  version: v1.0.0
paths:
  /api/admin/auditors:
    get:
      summary: index  admin auditors
      parameters:
      - name: name
        in: path
        schema:
          type: string
      - name: state
        in: path
        schema:
          type: string
      - name: labels
        in: path
        schema:
          type: string
      - name: certifications_types
        in: path
        schema:
          type: string
      - name: certification_body_id
        in: path
        schema:
          type: string
