openapi: 3.0.0
info:
  title: Admin Auditors Product
  version: v1.0.0
paths:
  /api/admin/auditors/:auditor_id/products:
    post:
      summary: create  
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                product:
                  type: object
                  properties:
                    product_id:
                      type: string
                    auditor_id:
                      type: string
