openapi: 3.0.0
info:
  title: Admin Auditors Product
  version: v1.0.0
paths:
  /api/admin/auditors/:auditor_id/products:
    get:
      summary: index  admin auditor products
      responses:
        200:
          description: 'success'
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  product:
                    type: string
