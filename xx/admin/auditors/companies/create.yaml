openapi: 3.0.0
info:
  title: Admin Auditors Company
  version: v1.0.0
paths:
  /api/admin/auditors/:auditor_id/companies:
    post:
      summary: create  
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                company:
                  type: object
                  properties:
                    auditor_id:
                      type: string
                    company_id:
                      type: string
