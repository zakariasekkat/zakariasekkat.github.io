openapi: 3.0.0
info:
  title: Admin Autocomplete
  version: v1.0.0
paths:
  /api/admin/autocompletes/certification_bodies:
    get:
      summary: certification_bodies  certification bodies admin autocompletes
      responses:
        200:
          description: 'success'
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  name:
                    type: string
