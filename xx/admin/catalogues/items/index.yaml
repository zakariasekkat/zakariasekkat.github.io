openapi: 3.0.0
info:
  title: Admin Catalogues Item
  version: v1.0.0
paths:
  /api/admin/catalogues/:catalogue_id/items:
    get:
      summary: index  admin catalogue items
      parameters:
      - name: catalogue_id
        in: path
        schema:
          type: string
      - name: name
        in: path
        schema:
          type: string
