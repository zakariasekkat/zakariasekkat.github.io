openapi: 3.0.0
info:
  title: Admin Sourcing journey
  version: v1.0.0
paths:
  /api/admin/sourcing_journeys/:id/geolocation:
    get:
      summary: geolocation  geolocation admin sourcing journey
      responses:
        200:
          description: 'success'
          content:
            application/json:
              schema:
                type: object
                properties:
                  geolocation:
                    type: string
