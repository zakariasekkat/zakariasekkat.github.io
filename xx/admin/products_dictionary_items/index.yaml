openapi: 3.0.0
info:
  title: Admin Products dictionary item
  version: v1.0.0
paths:
  /api/admin/products_dictionary_items:
    get:
      summary: index  admin products dictionary items
      responses:
        200:
          description: 'success'
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  product:
                    type: string
                  dictionary_item:
                    type: string
      parameters:
      - name: product_id
        in: path
        schema:
          type: string
      - name: dictionary_item_id
        in: path
        schema:
          type: string
