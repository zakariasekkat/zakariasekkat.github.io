openapi: 3.0.0
info:
  title: Admin Standards Dictionary item
  version: v1.0.0
paths:
  /api/admin/standards/:standard_id/dictionary_items:
    post:
      summary: create  
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                dictionary_item:
                  type: object
                  properties:
                    standard_id:
                      type: string
                    dictionary_item_id:
                      type: string
