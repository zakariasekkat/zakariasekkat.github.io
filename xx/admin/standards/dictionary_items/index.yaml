openapi: 3.0.0
info:
  title: Admin Standards Dictionary item
  version: v1.0.0
paths:
  /api/admin/standards/:standard_id/dictionary_items:
    get:
      summary: index  admin standard dictionary items
      responses:
        200:
          description: 'success'
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  dictionary_item:
                    type: string
