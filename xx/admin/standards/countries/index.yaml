openapi: 3.0.0
info:
  title: Admin Standards Country
  version: v1.0.0
paths:
  /api/admin/standards/:standard_id/countries:
    get:
      summary: index  admin standard countries
      responses:
        200:
          description: 'success'
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  country:
                    type: string
