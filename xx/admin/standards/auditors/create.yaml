openapi: 3.0.0
info:
  title: Admin Standards Auditor
  version: v1.0.0
paths:
  /api/admin/standards_auditors:
    post:
      summary: create  
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                standards_auditor:
                  type: object
                  properties:
                    auditor_id:
                      type: string
                    standard_id:
                      type: string
