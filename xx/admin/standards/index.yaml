openapi: 3.0.0
info:
  title: Admin Standard
  version: v1.0.0
paths:
  /api/admin/standards:
    get:
      summary: index  admin standards
      parameters:
      - name: name
        in: path
        schema:
          type: string
