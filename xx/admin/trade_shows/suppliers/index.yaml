openapi: 3.0.0
info:
  title: Admin Trade shows Supplier
  version: v1.0.0
paths:
  /api/admin/trade_shows/:trade_show_id/suppliers:
    get:
      summary: index  admin trade show suppliers
      parameters:
      - name: trade_show_id
        in: path
        schema:
          type: string
      - name: supplier_name
        in: path
        schema:
          type: string
