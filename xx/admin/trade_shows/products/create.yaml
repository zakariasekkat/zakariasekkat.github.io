openapi: 3.0.0
info:
  title: Admin Trade shows Product
  version: v1.0.0
paths:
  /api/admin/trade_shows/:trade_show_id/products:
    post:
      summary: create  
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                trade_show_product:
                  type: object
                  properties:
                    trade_show_id:
                      type: string
                    product_id:
                      type: string
                    position:
                      type: string
