openapi: 3.0.0
info:
  title: Admin Posts Catalogue
  version: v1.0.0
paths:
  /api/admin/posts/:post_id/catalogues:
    post:
      summary: create  
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                catalogue:
                  type: object
                  properties:
                    catalogue_id:
                      type: string
                    post_id:
                      type: string
