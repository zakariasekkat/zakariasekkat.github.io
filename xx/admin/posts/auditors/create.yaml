openapi: 3.0.0
info:
  title: Admin Posts Auditor
  version: v1.0.0
paths:
  /api/admin/posts/:post_id/auditors:
    post:
      summary: create  
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                auditor:
                  type: object
                  properties:
                    auditor_id:
                      type: string
                    post_id:
                      type: string
