openapi: 3.0.0
info:
  title: Admin Posts Company
  version: v1.0.0
paths:
  /api/admin/posts/:post_id/companies:
    post:
      summary: create  
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                company:
                  type: object
                  properties:
                    company_id:
                      type: string
                    post_id:
                      type: string
