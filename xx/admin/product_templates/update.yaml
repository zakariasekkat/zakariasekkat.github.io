openapi: 3.0.0
info:
  title: Admin Product template
  version: v1.0.0
paths:
  /api/admin/product_templates/:id:
    put:
      summary: update  
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                product_template:
                  type: object
                  properties:
                    name:
                      type: string
                    description:
                      type: string
