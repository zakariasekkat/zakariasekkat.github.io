openapi: 3.0.0
info:
  title: Admin Companies Tag
  version: v1.0.0
paths:
  /api/admin/companies/:company_id/tags:
    post:
      summary: create  
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                company_tag:
                  type: object
                  properties:
                    tag_id:
                      type: string
