openapi: 3.0.0
info:
  title: Admin Companies Tag
  version: v1.0.0
paths:
  /api/admin/companies/:company_id/tags:
    get:
      summary: index  admin company tags
      responses:
        200:
          description: 'success'
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  name:
                    type: string
