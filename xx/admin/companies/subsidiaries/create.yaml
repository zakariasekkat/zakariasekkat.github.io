openapi: 3.0.0
info:
  title: Admin Companies Subsidiary
  version: v1.0.0
paths:
  /api/admin/companies/:company_id/subsidiaries:
    post:
      summary: create  
      responses:
        200:
          description: 'success'
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  name:
                    type: string
                  logo:
                    type: string
