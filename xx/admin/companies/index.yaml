openapi: 3.0.0
info:
  title: Admin Company
  version: v1.0.0
paths:
  /api/admin/companies:
    get:
      summary: index  admin companies
      parameters:
      - name: name
        in: path
        schema:
          type: string
      - name: country_id
        in: path
        schema:
          type: string
      - name: state
        in: path
        schema:
          type: string
      - name: claimed
        in: path
        schema:
          type: string
