openapi: 3.0.0
info:
  title: Admin Companies User
  version: v1.0.0
paths:
  /api/admin/companies/:company_id/users:
    post:
      summary: create  
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                :
                  type: object
                  properties:
                    company_id:
                      type: string
                    user_id:
                      type: string
