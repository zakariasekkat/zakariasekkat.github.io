openapi: 3.0.0
info:
  title: Admin User category interest
  version: v1.0.0
paths:
  /api/admin/user_category_interest:
    get:
      summary: index  admin user category interest index
      responses:
        200:
          description: 'success'
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  category:
                    type: string
                  user:
                    type: string
                  created_at:
                    type: string
      parameters:
      
        in: path
        schema:
          type: string
