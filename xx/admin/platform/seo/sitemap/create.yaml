openapi: 3.0.0
info:
  title: Admin Platform Seo Sitemap
  version: v1.0.0
paths:
  /api/admin/platform/seo/sitemap:
    post:
      summary: create  
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                sitemap:
                  type: object
                  properties:
                    name:
                      type: string
                    context:
                      type: string
