openapi: 3.0.0
info:
  title: Admin Platform Image library
  version: v1.0.0
paths:
  /api/admin/platform/image_libraries:
    post:
      summary: create  
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                library:
                  type: object
                  properties:
                    name:
                      type: string
                    key:
                      type: string
                    context:
                      type: string
