openapi: 3.0.0
info:
  title: Admin Platform Companies type
  version: v1.0.0
paths:
  /api/admin/platform/companies_types:
    get:
      summary: index  admin platform companies types
      responses:
        200:
          description: 'success'
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  name:
                    type: string
                  position:
                    type: string
      parameters:
      - name: name
        in: path
        schema:
          type: string
