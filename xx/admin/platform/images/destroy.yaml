openapi: 3.0.0
info:
  title: Admin Platform Image
  version: v1.0.0
paths:
  /api/admin/platform/image_libraries/:library_id/images/:id:
    delete:
      summary: destroy  admin platform library image
