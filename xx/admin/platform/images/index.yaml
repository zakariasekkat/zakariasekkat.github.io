openapi: 3.0.0
info:
  title: Admin Platform Image
  version: v1.0.0
paths:
  /api/admin/platform/image_libraries/:library_id/images:
    get:
      summary: index  admin platform library images
      responses:
        200:
          description: 'success'
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  file:
                    type: string
                  state:
                    type: string
