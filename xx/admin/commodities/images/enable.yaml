openapi: 3.0.0
info:
  title: Admin Commodities Image
  version: v1.0.0
paths:
  /api/admin/commodities/:commodity_id/images/:id/enable:
    patch:
      summary: enable  enable admin commodity image
