openapi: 3.0.0
info:
  title: Admin Commodity
  version: v1.0.0
paths:
  /api/admin/commodities:
    post:
      summary: create  
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                commodity:
                  type: object
                  properties:
                    name:
                      type: string
                    slug:
                      type: string
