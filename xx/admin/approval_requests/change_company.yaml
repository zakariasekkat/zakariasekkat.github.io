openapi: 3.0.0
info:
  title: Admin Approval request
  version: v1.0.0
paths:
  /api/admin/approval_requests/:id/change_company:
    patch:
      summary: change_company  change company admin approval request
