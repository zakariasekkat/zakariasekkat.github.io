openapi: 3.0.0
info:
  title: Admin Approval request
  version: v1.0.0
paths:
  /api/admin/approval_requests:
    get:
      summary: index  admin approval requests
      parameters:
      - name: approver_id
        in: path
        schema:
          type: string
      - name: state
        in: path
        schema:
          type: string
