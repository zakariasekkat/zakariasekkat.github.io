openapi: 3.0.0
info:
  title: Admin Type
  version: v1.0.0
paths:
  /api/admin/types:
    post:
      summary: create  
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                label_type:
                  type: object
                  properties:
                    name:
                      type: string
