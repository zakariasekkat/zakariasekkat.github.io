openapi: 3.0.0
info:
  title: Admin Type
  version: v1.0.0
paths:
  /api/admin/types/:id:
    put:
      summary: update  
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                label_type:
                  type: object
                  properties:
      :
                      type: string
