openapi: 3.0.0
info:
  title: Admin Type
  version: v1.0.0
paths:
  /api/admin/types:
    get:
      summary: index  admin types
      responses:
        200:
          description: 'success'
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  name:
                    type: string
