openapi: 3.0.0
info:
  title: Admin Supplier join request
  version: v1.0.0
paths:
  /api/admin/supplier_join_requests/:id/accept:
    patch:
      summary: accept  accept admin supplier join request
