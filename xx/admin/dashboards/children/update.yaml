openapi: 3.0.0
info:
  title: Admin Dashboards Child
  version: v1.0.0
paths:
  /api/admin/dashboards/:dashboard_id/children/:id:
    put:
      summary: update  
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                dashboard:
                  type: object
                  properties:
      :
                      type: string
