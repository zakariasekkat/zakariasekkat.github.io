openapi: 3.0.0
info:
  title: Admin Dashboard
  version: v1.0.0
paths:
  /api/admin/dashboards:
    post:
      summary: create  
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                dashboard:
                  type: object
                  properties:
                    name:
                      type: string
