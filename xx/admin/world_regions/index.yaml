openapi: 3.0.0
info:
  title: Admin World region
  version: v1.0.0
paths:
  /api/admin/world_regions:
    get:
      summary: index  admin world regions
      parameters:
      - name: name
        in: path
        schema:
          type: string
