openapi: 3.0.0
info:
  title: Dashboard Scope certificates File
  version: v1.0.0
paths:
  /api/dashboard/certificates/scope_certificates/:scope_certificate_id/files:
    get:
      summary: index  dashboard certificates scope certificate files
      responses:
        200:
          description: 'success'
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  upload_url:
                    type: string
                  upload_content_type:
                    type: string
