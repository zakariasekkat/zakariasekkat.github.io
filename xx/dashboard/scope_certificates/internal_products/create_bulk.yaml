openapi: 3.0.0
info:
  title: Dashboard Scope certificates Internal product
  version: v1.0.0
paths:
  /api/dashboard/certificates/scope_certificates/:scope_certificate_id/internal_products/create_bulk:
    post:
      summary: create_bulk  create bulk dashboard certificates scope certificate internal products
