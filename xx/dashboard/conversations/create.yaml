openapi: 3.0.0
info:
  title: Dashboard Conversation
  version: v1.0.0
paths:
  /api/internal/companies/conversations:
    post:
      summary: create  
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                conversation:
                  type: object
                  properties:
                    body:
                      type: string
                    contact_id:
                      type: string
