openapi: 3.0.0
info:
  title: Dashboard Product standard
  version: v1.0.0
paths:
  /api/dashboard/products/:product_id/variants/:variant_id/standards:
    post:
      summary: create  
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                standard:
                  type: object
                  properties:
                    standard_id:
                      type: string
                    number:
                      type: string
