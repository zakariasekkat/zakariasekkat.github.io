openapi: 3.0.0
info:
  title: Dashboard Product image
  version: v1.0.0
paths:
  /api/dashboard/products/:product_id/variants/:variant_id/images/:id:
    delete:
      summary: destroy  dashboard product variant product image
