openapi: 3.0.0
info:
  title: Dashboard Product image
  version: v1.0.0
paths:
  /api/dashboard/products/:product_id/variants/:variant_id/images:
    post:
      summary: create  
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                image:
                  type: object
                  properties:
                    file:
                      type: string
