openapi: 3.0.0
info:
  title: Dashboard Posts Catalogue
  version: v1.0.0
paths:
  /api/dashboard/posts/:post_id/catalogues:
    post:
      summary: create  
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                catalogue:
                  type: object
                  properties:
                    catalogue_id:
                      type: string
