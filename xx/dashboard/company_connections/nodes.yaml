openapi: 3.0.0
info:
  title: Dashboard Company connection
  version: v1.0.0
paths:
  /api/dashboard/connections/nodes:
    get:
      summary: nodes  nodes dashboard connections
      responses:
        200:
          description: 'success'
          content:
            application/json:
              schema:
                type: object
                properties:
                  nodes:
                    type: string
                  edges:
                    type: string
