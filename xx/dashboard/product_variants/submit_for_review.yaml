openapi: 3.0.0
info:
  title: Dashboard Product variant
  version: v1.0.0
paths:
  /api/dashboard/products/:product_id/variants/:id/submit_for_review:
    post:
      summary: submit_for_review  submit for review dashboard product variant
