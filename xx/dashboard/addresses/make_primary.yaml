openapi: 3.0.0
info:
  title: Dashboard Address
  version: v1.0.0
paths:
  /api/internal/companies/addresses/:id/make_primary:
    patch:
      summary: make_primary  make primary internal companies address
