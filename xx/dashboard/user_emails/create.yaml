openapi: 3.0.0
info:
  title: Dashboard User email
  version: v1.0.0
paths:
  /api/dashboard/user_emails:
    post:
      summary: create  
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                email:
                  type: object
                  properties:
                    unconfirmed_email:
                      type: string
