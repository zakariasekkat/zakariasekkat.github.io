openapi: 3.0.0
info:
  title: Dashboard Catalogues Image
  version: v1.0.0
paths:
  /api/dashboard/catalogues/:catalogue_id/images:
    get:
      summary: index  dashboard catalogue images
      responses:
        200:
          description: 'success'
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  name:
                    type: string
                  file_url:
                    type: string
