openapi: 3.0.0
info:
  title: Dashboard Standard owner dashboard Auditor
  version: v1.0.0
paths:
  /api/dashboard/managed_auditors:
    post:
      summary: create  
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                certification_body_auditor:
                  type: object
                  properties:
                    connection_node_id:
                      type: string
