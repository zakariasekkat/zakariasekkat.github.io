openapi: 3.0.0
info:
  title: Dashboard Standard owner dashboard Company
  version: v1.0.0
paths:
  /api/dashboard/certified_companies:
    post:
      summary: create  
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                certification_body_company:
                  type: object
                  properties:
                    connection_node_id:
                      type: string
