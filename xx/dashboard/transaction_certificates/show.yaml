openapi: 3.0.0
info:
  title: Dashboard Transaction certificate
  version: v1.0.0
paths:
  /api/dashboard/certificates/transaction_certificates/:id:
    get:
      summary: show  dashboard certificates transaction certificate
