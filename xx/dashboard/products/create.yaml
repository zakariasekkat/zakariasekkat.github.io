openapi: 3.0.0
info:
  title: Dashboard Product
  version: v1.0.0
paths:
  /api/dashboard/products:
    post:
      summary: create  
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                product:
                  type: object
                  properties:
                    name:
                      type: string
                    category_id:
                      type: string
