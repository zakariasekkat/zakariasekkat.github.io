openapi: 3.0.0
info:
  title: Dashboard Company capability
  version: v1.0.0
paths:
  /api/dashboard/company_capabilities:
    get:
      summary: index  dashboard company capabilities
      responses:
        200:
          description: 'success'
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  name:
                    type: string
                  slug:
                    type: string
      parameters:
      
        in: path
        schema:
          type: string
